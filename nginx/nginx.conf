events {
    worker_connections 1024;
}

stream {
    # 如果需要 TCP 转发（如 MySQL、LDAP）
    # upstream mysql {
    #     server 192.168.20.164:3306;
    # }
    # server {
    #     listen 3306;
    #     proxy_pass mysql;
    # }
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;
    gzip on;

    # 上游服务
    upstream lubase_gateway {
        server lubase-gateway:7573;
    }

    upstream lubase_frontend {
        server lubase-frontend:80;
    }

    # HTTP 服务器（重定向到 HTTPS）
    server {
        listen 80;
        server_name _;

        # GitHub OAuth 回调白名单 - 不重定向到 HTTPS
        location /login/oauth2/code/github {
            proxy_pass http://lubase_gateway;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 健康检查
        location /health {
            return 200 "OK";
            add_header Content-Type text/plain;
        }

        # 重定向到 HTTPS（生产环境启用）
        # return 301 https://$host$request_uri;
    }

    # HTTPS 服务器（生产环境使用）
    # server {
    #     listen 443 ssl http2;
    #     server_name lubase.frp.zhenjiucheng.net;
    #
    #     # SSL 证书配置（使用 Let's Encrypt）
    #     ssl_certificate /etc/letsencrypt/live/lubase.frp.zhenjiucheng.net/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/lubase.frp.zhenjiucheng.net/privkey.pem;
    #     ssl_session_timeout 1d;
    #     ssl_session_cache shared:SSL:50m;
    #     ssl_session_tickets off;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    #     ssl_prefer_server_ciphers off;
    #     add_header Strict-Transport-Security "max-age=63072000" always;
    #
    #     # 前端静态文件
    #     location / {
    #         proxy_pass http://lubase_frontend;
    #     }
    #
    #     # API 转发到 Gateway
    #     location /uaa {
    #         proxy_pass http://lubase_gateway;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #
    #     location /product {
    #         proxy_pass http://lubase_gateway;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #
    #     # GitHub OAuth 回调
    #     location /login/oauth2/code/github {
    #         proxy_pass http://lubase_gateway;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
